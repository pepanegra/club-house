# .env — Variables de entorno (NUNCA subir a GitHub)
# Agrega este archivo a .gitignore

SECRET_KEY=django-insecure-clubhouse-dev-key-2026-cambia-esto-en-produccion
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:5500,http://127.0.0.1:8080

# En Railway agrega esto:
# DATABASE_URL=postgresql://usuario:password@host:port/db
